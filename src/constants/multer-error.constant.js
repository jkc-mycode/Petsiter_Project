import { MULTER_MESSAGE } from './multer-message.constant.js';

export const multerErrorMessages = {
  LIMIT_PART_COUNT: MULTER_MESSAGE.PART_COUNT,
  LIMIT_FILE_SIZE: MULTER_MESSAGE.FILE_SIZE,
  LIMIT_FILE_COUNT: MULTER_MESSAGE.FILE_COUNT,
  LIMIT_FIELD_KEY: MULTER_MESSAGE.FIELD_KEY,
  LIMIT_FIELD_VALUE: MULTER_MESSAGE.FIELD_VALUE,
  LIMIT_FIELD_COUNT: MULTER_MESSAGE.FIELD_COUNT,
  LIMIT_UNEXPECTED_FILE: MULTER_MESSAGE.UNEXPECTED_FILE,
  MISSING_FIELD_NAME: MULTER_MESSAGE.FIELD_NAME,
};
